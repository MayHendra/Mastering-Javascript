const num1 = 100;
const num2 = 50;
let val;

// simple math operator
val = num1 + num2;

console.log(val);

// Math Object

val = Math.PI;
val = Math.E;
val = Math.round(3.14);

// Pembulatan angka keatas
val = Math.ceil(3.14);

// Pembulatan angka kebawah
val = Math.floor(3.14);

// Mencari akar kuadrat
val = Math.sqrt(64);

// Mengubah nilai negatif menjadi absolute
val = Math.abs(-5);

console.log(val);
